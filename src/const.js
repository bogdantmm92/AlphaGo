let SERVER_URL = "http://192.168.1.230:3000";

const handPickerWidth = 60;
const handPickerHeight = 60;
const stoneSize = 16;
const lineHeight = 2;
const padding = 10;

export {
	SERVER_URL,

	handPickerWidth,
	handPickerHeight,
	stoneSize,
	lineHeight,
	padding
};
